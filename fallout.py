codeRun = 0
code = input("Welcome to Nuclear Fallout Decoder V.7.6 Please insert code.")
code = code.upper()

def printCode():
  codeRun += 1
  tempCode = code[:codeRun]
  tempCode2 = code[codeRun:]

code = code.replace("W", "A")
code.replace("H", "B")
code.replace
print(code.replace())